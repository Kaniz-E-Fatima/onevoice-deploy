@import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600;700;800&family=DM+Sans:wght@400;500;600;700&display=swap');

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'DM Sans', sans-serif;
    background: #fff;
    color: #1a1a1a;
}

/* HEADER */
.home-header {
    background: #8B0000;
    padding: 0 40px;
    position: sticky;
    top: 0;
    z-index: 100;
    box-shadow: 0 2px 20px rgba(0, 0, 0, 0.2);
}

.header-inner {
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 70px;
}

.header-logo {
    display: flex;
    align-items: center;
    gap: 14px;
}

.logo-circle {
    width: 44px;
    height: 44px;
    border-radius: 50%;
    background: white;
    color: #8B0000;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: 'Playfair Display', serif;
    font-weight: 700;
    font-size: 14px;
}

.logo-title {
    font-family: 'Playfair Display', serif;
    font-size: 18px;
    font-weight: 700;
    color: white;
}

.logo-sub {
    font-size: 10px;
    color: rgba(255, 255, 255, 0.7);
    letter-spacing: 0.3px;
}

.header-nav {
    display: flex;
    gap: 28px;
}

.header-nav a {
    color: rgba(255, 255, 255, 0.85);
    text-decoration: none;
    font-size: 14px;
    font-weight: 500;
    transition: color 0.2s;
}

.header-nav a:hover {
    color: white;
}

/* HERO */
.hero-section {
    position: relative;
    height: 520px;
    background: linear-gradient(135deg, #8B0000 0%, #1a1a1a 60%, #3a0000 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
}

.hero-section::before {
    content: '';
    position: absolute;
    inset: 0;
    background: url('https://images.unsplash.com/photo-1562774053-701939374585?w=1400&q=80') center/cover;
    opacity: 0.15;
}

.hero-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(139, 0, 0, 0.85) 0%, rgba(26, 26, 26, 0.9) 100%);
}

.hero-content {
    position: relative;
    text-align: center;
    color: white;
    padding: 0 20px;
    max-width: 800px;
}

.hero-badge {
    display: inline-block;
    background: rgba(255, 255, 255, 0.15);
    border: 1px solid rgba(255, 255, 255, 0.3);
    padding: 6px 18px;
    border-radius: 20px;
    font-size: 12px;
    letter-spacing: 0.5px;
    margin-bottom: 20px;
}

.hero-title {
    font-family: 'Playfair Display', serif;
    font-size: 52px;
    font-weight: 800;
    line-height: 1.1;
    margin-bottom: 16px;
}

.hero-title span {
    color: #ffaaaa;
}

.hero-subtitle {
    font-size: 17px;
    color: rgba(255, 255, 255, 0.8);
    margin-bottom: 32px;
    line-height: 1.6;
}

.hero-buttons {
    display: flex;
    gap: 14px;
    justify-content: center;
    flex-wrap: wrap;
}

.btn-primary {
    background: white;
    color: #8B0000;
    padding: 12px 28px;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 600;
    font-size: 14px;
    transition: all 0.2s;
}

.btn-primary:hover {
    background: #ffeeee;
    transform: translateY(-2px);
}

.btn-secondary {
    background: transparent;
    color: white;
    padding: 12px 28px;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 600;
    font-size: 14px;
    border: 2px solid rgba(255, 255, 255, 0.5);
    transition: all 0.2s;
}

.btn-secondary:hover {
    border-color: white;
    transform: translateY(-2px);
}

/* STATS */
.stats-section {
    background: #1a1a1a;
    padding: 40px 40px;
}

.stats-inner {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 20px;
}

.stat-card {
    text-align: center;
    padding: 24px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    background: rgba(255, 255, 255, 0.05);
}

.stat-number {
    font-family: 'Playfair Display', serif;
    font-size: 32px;
    font-weight: 700;
    color: #ff6b6b;
}

.stat-label {
    font-size: 13px;
    color: rgba(255, 255, 255, 0.6);
    margin-top: 6px;
}

/* CHATBOT INFO */
.chatbot-info-section {
    padding: 80px 40px;
    background: #fff;
}

.chatbot-info-inner {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 60px;
    align-items: center;
}

.section-badge {
    display: inline-block;
    background: #fff0f0;
    color: #8B0000;
    padding: 6px 16px;
    border-radius: 20px;
    font-size: 12px;
    font-weight: 600;
    margin-bottom: 16px;
    border: 1px solid #ffcccc;
}

.chatbot-info-text h2 {
    font-family: 'Playfair Display', serif;
    font-size: 38px;
    font-weight: 700;
    color: #1a1a1a;
    margin-bottom: 16px;
}

.chatbot-info-text h2 span {
    color: #8B0000;
}

.chatbot-info-text p {
    font-size: 15px;
    color: #555;
    line-height: 1.7;
    margin-bottom: 24px;
}

.feature-list {
    display: flex;
    flex-direction: column;
    gap: 10px;
    margin-bottom: 24px;
}

.feature-item {
    font-size: 14px;
    color: #333;
    padding: 8px 14px;
    background: #f9f9f9;
    border-radius: 8px;
    border-left: 3px solid #8B0000;
}

.try-hint {
    font-size: 14px;
    color: #8B0000;
    font-weight: 600;
}

/* Demo Card */
.info-card {
    background: white;
    border-radius: 18px;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.12);
    overflow: hidden;
    border: 1px solid #f0f0f0;
}

.info-card-header {
    background: linear-gradient(135deg, #8B0000, #A50000);
    color: white;
    padding: 16px 20px;
    font-weight: 600;
    font-size: 15px;
}

.info-card-body {
    padding: 20px;
    display: flex;
    flex-direction: column;
    gap: 10px;
    background: #f9f9f9;
}

.demo-msg {
    padding: 10px 14px;
    border-radius: 10px;
    font-size: 13px;
    max-width: 85%;
    line-height: 1.5;
}

.demo-msg.bot {
    background: white;
    color: #1a1a1a;
    align-self: flex-start;
    border-left: 3px solid #8B0000;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
}

.demo-msg.user {
    background: linear-gradient(135deg, #8B0000, #A50000);
    color: white;
    align-self: flex-end;
}

.info-card-langs {
    padding: 14px 20px;
    display: flex;
    gap: 8px;
    background: white;
    border-top: 1px solid #f0f0f0;
}

.info-card-langs span {
    padding: 4px 12px;
    background: #fff0f0;
    color: #8B0000;
    border-radius: 20px;
    font-size: 12px;
    font-weight: 600;
    border: 1px solid #ffcccc;
}

/* ABOUT */
.about-section {
    padding: 80px 40px;
    background: #f9f9f9;
}

.about-inner {
    max-width: 1200px;
    margin: 0 auto;
}

.about-inner h2 {
    font-family: 'Playfair Display', serif;
    font-size: 38px;
    font-weight: 700;
    color: #1a1a1a;
    text-align: center;
    margin-bottom: 40px;
}

.about-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 24px;
}

.about-card {
    background: white;
    border-radius: 14px;
    padding: 28px 24px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.06);
    border-top: 4px solid #8B0000;
    transition: transform 0.2s;
}

.about-card:hover {
    transform: translateY(-4px);
}

.about-icon {
    font-size: 32px;
    margin-bottom: 14px;
}

.about-card h3 {
    font-family: 'Playfair Display', serif;
    font-size: 17px;
    font-weight: 700;
    color: #1a1a1a;
    margin-bottom: 10px;
}

.about-card p {
    font-size: 13.5px;
    color: #666;
    line-height: 1.6;
}

/* FOOTER */
.home-footer {
    background: #1a1a1a;
    color: white;
    padding: 40px 40px 0;
}

.footer-inner {
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    gap: 40px;
    padding-bottom: 32px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    flex-wrap: wrap;
}

.footer-logo {
    font-family: 'Playfair Display', serif;
    font-size: 20px;
    font-weight: 700;
    color: white;
    margin-bottom: 6px;
}

.footer-sub {
    font-size: 12px;
    color: rgba(255, 255, 255, 0.5);
    margin-bottom: 8px;
}

.footer-address {
    font-size: 13px;
    color: rgba(255, 255, 255, 0.6);
}

.footer-right {
    display: flex;
    flex-direction: column;
    gap: 8px;
    font-size: 13px;
    color: rgba(255, 255, 255, 0.6);
}

.footer-bottom {
    max-width: 1200px;
    margin: 0 auto;
    padding: 16px 0;
    font-size: 12px;
    color: rgba(255, 255, 255, 0.4);
    text-align: center;
}

/* RESPONSIVE */
@media (max-width: 768px) {
    .hero-title {
        font-size: 32px;
    }

    .stats-inner {
        grid-template-columns: repeat(2, 1fr);
    }

    .chatbot-info-inner {
        grid-template-columns: 1fr;
    }

    .about-grid {
        grid-template-columns: repeat(2, 1fr);
    }

    .header-nav {
        display: none;
    }

    .hero-section {
        height: auto;
        padding: 60px 20px;
    }
}

@media (max-width: 480px) {
    .about-grid {
        grid-template-columns: 1fr;
    }

    .stats-inner {
        grid-template-columns: repeat(2, 1fr);
    }
}

.logo-img {
    width: 44px;
    height: 44px;
    border-radius: 50%;
    object-fit: cover;
    border: 2px solid rgba(255, 255, 255, 0.6);
}